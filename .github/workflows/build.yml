name: Build Splix Drivers

on:
  push:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.deb_arch }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          # ARM64 (Raspberry Pi) build using Debian Bookworm
          - arch: aarch64
            distro: bookworm
            deb_arch: arm64
          # x64 (Standard PC/Server) build using Debian Bookworm
          - arch: x86_64
            distro: bookworm
            deb_arch: amd64

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Compile and Build .deb inside emulated environment
        uses: uraimo/run-on-arch-action@v2
        id: build
        with:
          arch: ${{ matrix.arch }}
          distro: ${{ matrix.distro }}
          githubToken: ${{ github.token }}
          
          # Create a shared folder to extract the finished .deb file
          setup: |
            mkdir -p "${PWD}/artifacts"
          dockerRunArgs: |
            --volume "${PWD}/artifacts:/artifacts"
            
          # Install dependencies, fix linker, and compile
          run: |
            apt-get update -y
            apt-get install -y build-essential libcups2-dev libcupsimage2-dev libjbig-dev cups-ppdc recode checkinstall
            
            # Fix the Debian JBIG linker issue
            find . -type f -name "*.mk" -exec sed -i 's/-ljbig85/-ljbig/g' {} +
            
            make clean
            make
            make drv
            make ppd
            
            # Wrap into .deb
            checkinstall -D --install=no --default \
              --pkgname="splix" \
              --pkgversion="2.0.2" \
              --maintainer="github-actions" \
              --pkgarch="${{ matrix.deb_arch }}" \
              --requires="cups, libcups2, libcupsimage2, libjbig0" \
              make install
              
            cp *.deb /artifacts/

      - name: Upload .deb Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: splix-deb-${{ matrix.deb_arch }}
          path: artifacts/*.deb